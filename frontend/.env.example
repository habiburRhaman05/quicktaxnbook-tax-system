# Public app URL (used by SEO and cookie settings).
NEXT_PUBLIC_APP_URL=http://localhost:6767

# Express API base URL - server-side only, the browser never talks to this
# directly. All authenticated requests go through Next.js route handlers
# under /api/* which proxy to this URL with the session cookie's token.
EXPRESS_API_URL=http://localhost:8000/v1
